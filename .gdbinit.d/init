dashboard source -style height 15
# !MODUL sorgt dafür, dass man MODUL erst hinzuschalten muss
#   Man kann alle hier definierten per 'dashboard MODUL' an-/ausschalten
# Reihenfolge hier entspricht der Dargestellten
dashboard -layout expressions history source variables !stack !threads !breakpoints

# Load upas-s5p/cobra .gdbinit automatically
# Loads this file always
# This produces an omitted error because I use 'file PROGRAM' and may not exist
# add-auto-load-safe-path ~/proj/upas-s5p/UPAS-L2/build/verbose/cobra.gdbinit

# Quit without confirmation
# 'set confirm off' disables confirmation generally (which I dont want) but since I am quitting gdb anyway this doesnt make any difference
define hook-quit
    set confirm off
end
