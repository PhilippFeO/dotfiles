When creating code, always add semantically explainatory comments.

When creating a function, always add a docstring. If it's a Python function, use the Google docstring format.

When you update code, update related comments and docstrings.
